# Server
PORT=5000

# JWT Secret - use a strong random string in production
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# MySQL Database (use Cloud MySQL URL for production e.g. PlanetScale, Railway)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=minilinkedin

# Frontend URL for CORS (e.g. http://localhost:5173 for Vite dev)
CLIENT_URL=http://localhost:5173
